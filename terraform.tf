terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.92"
    }
  }

  required_version = ">= 1.2"
}
# 지역 선언
provider "aws" {
  region = "ap-northeast-2"
}